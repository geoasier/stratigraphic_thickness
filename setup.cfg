[flake8]
max-line-length = 120
# Archivos generados automaticamente o boilerplate del Plugin Builder:
# no se editan a mano, asi que no tiene sentido lintarlos.
exclude =
    .git,
    __pycache__,
    help,
    test,
    scripts,
    i18n
